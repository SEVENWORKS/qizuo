#最新加载入口配置文件

#spring配置
spring:

#springboot配置
spring.boot:

#springcloud配置
spring.cloud:
    #config配置
    config:
      server:
        #本地文件配置
        #映射说明如下
        #/{application}/{profile}[/{label}]
        #/{application}-{profile}.yml
        #/{label}/{application}-{profile}.yml
        #/{application}-{profile}.properties
        #/{label}/{application}-{profile}.properties
        native.search-locations: C:/IdeaProjets/demo_configs/
        #线上配置
        git:
          uri: https://github.com/SEVENWORKS/qizuo.git #仓库地址
          username: a554657207@126.com #仓库用户名
          password: 9a,1a,5a,1a,4a, #仓库密码
          search-paths: /qizuo/qizuo-config/src/main/resources/config/ #git仓库下相对地址
          default-label: ${spring.profiles.active} #仓库默认分支
          #basedir: /data/config #本地仓库配置

#eureka注册中心配置
eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.cloud.client.ipAddress}:${server.port} #注册服务信息
  client:
    serviceUrl:
      defaultZone: http://qizuo:qizuo@localhost:9100/eureka #注册中心地址 前面是springserity的账号密码

#config配置加密
encrypt:
  key-store:
    location: classpath:/server.jks
    password: paascloud-storepass
    alias: paascloud-key-store
    secret: paascloud-keypass